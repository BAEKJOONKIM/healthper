<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="egovframework.healthper.mapper.MemberMapper">
	
	<select id="memberLogin" parameterType="memberVO" resultType="memberVO">
		SELECT * FROM member WHERE M_ID=#{mId} AND M_PW=#{mPw}
	</select>
	
	<select id="memberFindTrainerList" parameterType="trainerVO" resultType="trainerVO">
		
	</select>
	
</mapper>